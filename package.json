{
	"name": "showdown-vgc-team-sheet-extension",
	"description": "An extension to export team to a VGC sheet on Pokémon Showdown.",
	"version": "0.5.0",
	"author": "Kasper Wissendorf",
	"license": "MIT",
	"main": "src/background.ts",
	"scripts": {
		"watch": "webpack --watch",
		"start": "webpack serve",
		"format": "prettier --write .",
		"build:all": "npm run build:chrome && npm run build:firefox",
		"build:chrome": "cd scripts && build.sh chrome",
		"build:firefox": "cd scripts && build.sh firefox"
	},
	"keywords": [
		"browser",
		"extension",
		"typescript"
	],
	"devDependencies": {
		"@types/chrome": "^0.1.24",
		"copy-webpack-plugin": "^13.0.1",
		"prettier": "3.6.2",
		"ts-loader": "^8.0.0",
		"typescript": "^4.0.0",
		"webpack": "^5.102.1",
		"webpack-cli": "^5.1.4"
	},
	"dependencies": {
		"pdf-lib": "^1.17.1"
	}
}
